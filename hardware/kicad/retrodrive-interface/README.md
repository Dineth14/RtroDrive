# KiCad capture gate

The [Rev-A architecture](../../../docs/hardware/rev-a-schematic-architecture.md) defines the intended00–11 sheet hierarchy. No `.kicad_sch` or `.kicad_pcb` is claimed complete. Final companion part/pins, protection ratings/values, connector footprints, stackup and mechanical outline are unresolved. Placeholder CAD would not constitute a buildable product.

Capture reviewed circuitry in KiCad only after these decisions close, retain engineering notes on each sheet, run ERC/DRC with a pinned tool version, and use the manufacturing checklist before a tagged release. The current release workflow deliberately fails closed.
