name: retrodrive_mobile
description: RetroDrive companion foundation with shared profiles and simulated telemetry.
publish_to: none
version: 0.1.0+1
environment:
  sdk: '>=3.9.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - assets/profiles.json
    - assets/demo-telemetry.json
