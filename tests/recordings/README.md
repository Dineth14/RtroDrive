# Recordings

`demo.jsonl` is deterministically generated **synthetic** canonical telemetry, usable through the web recording input. It is not a vehicle capture. Real traces require source/harness/ECU identity, timing, anonymization and validation metadata before inclusion.
